import { Typography } from '@mui/material';
import PageLayout from '../../components/layout/PageLayout';

const Users = (): JSX.Element => {
    return (
        <PageLayout title='Users'>
            <Typography>Users page content.</Typography>
        </PageLayout>
    );
};

export default Users;
