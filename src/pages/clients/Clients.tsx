import { Typography } from '@mui/material';
import PageLayout from '../../components/layout/PageLayout';

const Clients = (): JSX.Element => {
    return (
        <PageLayout title='Clients'>
            <Typography>Clients page content.</Typography>
        </PageLayout>
    );
};

export default Clients;
